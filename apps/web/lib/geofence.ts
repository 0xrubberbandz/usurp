// Stub only. TODO: real server-side geofencing, jurisdiction policy, and legal
// review are required before mainnet. A client-side gate is not access control.
export function isCountryBlocked(_countryCode?: string): boolean {
  return false;
}
