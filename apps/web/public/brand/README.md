User-provided USURP crown artwork. `crown.png` is the original transparent PNG.
`crown.svg` is the supplied SVG container with embedded raster artwork, not a
resolution-independent vector. Used unchanged as the animated throne crown.
